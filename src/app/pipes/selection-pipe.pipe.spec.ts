import { SelectionPipePipe } from './selection-pipe.pipe';

describe('SelectionPipePipe', () => {
  it('create an instance', () => {
    const pipe = new SelectionPipePipe();
    expect(pipe).toBeTruthy();
  });
});
